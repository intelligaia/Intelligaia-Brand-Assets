# whitepapers

Published whitepapers live in this folder. Drop new files here and
regenerate `assets/manifest.json` so the site and any agent pick them up.

Accepted: `.html` (self-contained), `.pdf`, `.png`
Naming: `<Subject> — <Client or context>.<ext>`
