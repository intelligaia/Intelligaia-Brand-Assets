# User Story Library

Store normalized CPQ stories by owning platform or under `cross-platform/` when the behavior spans systems.

Use `story-template.md`. Every story should identify persona, outcome, rules, data, dependencies, acceptance criteria, exceptions, and open questions. Avoid embedding unsupported implementation assumptions.
